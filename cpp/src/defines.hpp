#ifndef DEFINES_HPP				// This is a header guard. Used to prevent the header from being included more than once.
	#define DEFINES_HPP

// Platform macros are now derived from compiler-provided predefined macros.
// Do NOT define WINDOWS or VISUAL_STUDIO unconditionally; that breaks
// cross-platform builds.  Use _WIN32, _WIN64, _MSC_VER in code instead,
// or the WINDOWS macro defined conditionally in constant.hpp.

namespace pensar_digital
{
	namespace cpplib
	{
	}
}

#endif	// DEFINES_HPP


